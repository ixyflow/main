# Premium TechNoir — Website

Static HTML/CSS/JS website for Premium TechNoir.

## Structure

```
premium-technoir-website/
├── index.html
├── styles.css
├── script.js
└── .gitignore
```

## Getting Started

Open `index.html` directly in a browser, or serve with any static file server:

```bash
npx serve .
```

## Initialize Git

```bash
git init
git add .
git commit -m "Initial commit"
```
